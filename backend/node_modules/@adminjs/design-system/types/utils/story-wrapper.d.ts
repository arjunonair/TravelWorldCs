import React from 'react';
import { BoxProps } from '..';
export declare type StoryWrapperProps = BoxProps & {
    label: string;
    children?: React.ReactNode;
};
declare const StoryWrapper: React.FC<StoryWrapperProps>;
export default StoryWrapper;
